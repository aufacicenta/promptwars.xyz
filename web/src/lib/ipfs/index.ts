import upload, { getFileAsIPFSUrl } from "./upload";
import asHttpsURL from "./asHttpsURL";
import fetch from "./fetch";

const e = {
  fetch,
  upload,
  asHttpsURL,
  getFileAsIPFSUrl,
};

export default e;
